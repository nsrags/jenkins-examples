pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'export PATH=$PATH:/Users/nsraghavan/apache-maven-3.9.9/bin'
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}