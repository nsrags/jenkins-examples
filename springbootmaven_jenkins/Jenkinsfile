pipeline {
    agent any
    tools{
        maven 'MavenHome'
    }
    stages {
        stage('Build') { 
            steps {
                dir("./springbootmaven_jenkins") {
                    mvn clean package
                }
            }
        }
    }
}