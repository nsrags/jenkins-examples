pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                withMaven(
                     maven: 'MavenHome'
                ){
                    sh "mvn clean verify"
                }
            }
        }
    }
}