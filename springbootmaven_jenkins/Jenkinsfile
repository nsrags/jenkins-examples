pipeline {
    agent any
    tools{
        maven 'MavenHome'
    }
    stages {
        stage('Build') { 
            steps {
                sh "ls -l"
                sh "mvn clean package"
            }
        }
    }
}